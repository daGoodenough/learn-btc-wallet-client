export default `
# Generate a Private Key
---
Private keys are **really* big numbers (256 bits) generate at random.

One could even generate a private key by rolling dice or flipping a coin.

The odds of guessing someone's private key are infinitesimally small because there are 2 ^ 256 possibilities.
`;
