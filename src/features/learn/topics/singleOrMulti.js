const singleOrMulti = `
## Single Signature vs. Multiple Signatures
---
A **single sig** address is one that only require *one signature* to spend from.

A **multi-sig** address requires *multiple signatures* to spend.

The question being asked is "Do you want to use one, or multiple keys to control the funds spent to this address"?
`;

export default singleOrMulti;
